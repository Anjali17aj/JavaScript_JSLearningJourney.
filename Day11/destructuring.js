//destructuring

let names = ["tony","peter","Steve","captain","chiky"];
let winner = names[0];
let runnerup = names[1];
let secondRunnerup = names[2];

              //or
//let [ winner, runnerup, secondRunnerup, other] = names;
               //or
//let [ winner, runnerup, secondRunnerup, ...other] = names;